import React, {useEffect} from 'react';

const FirebaseUI = () => {
    // useEffect(()=>{

    // },[])

    return (
        <div className="container">
            Enter Phone Number
        </div>
    )
};

export default FirebaseUI;